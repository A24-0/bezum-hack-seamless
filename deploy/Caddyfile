# {$DOMAIN} — set via environment (docker-compose.prod.yml).
# Public domain: Caddy obtains Let's Encrypt certificates automatically (ports 80/443 must be reachable).
# localhost: HTTP on :80; optional HTTPS with internal/local trust.

{$DOMAIN:localhost} {
	reverse_proxy frontend:80
}
